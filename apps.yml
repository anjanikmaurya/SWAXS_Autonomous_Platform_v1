# SWAXS Platform — App Registry
# ──────────────────────────────────────────────────────────────────────────────
# Add new apps here. The Hub reads this file on startup and launches each app
# as an independent subprocess. No code changes needed to register a new app.
#
# Required fields per entry:
#   id          — unique slug used internally (no spaces)
#   name        — display name shown in the Hub UI
#   port        — TCP port the app listens on
#   entry       — path to app.py relative to the project root
#
# Optional fields (used by Hub UI and AI subsystem):
#   description — one-line description for the Hub card
#   icon        — emoji shown in the Hub card
#   color       — hex colour for the Hub card accent
#   knowledge   — path to a knowledge.md the AI indexes on registration
#   manifest_key — top-level key in manifest.json this app owns
# ──────────────────────────────────────────────────────────────────────────────

apps:
  - id:           calibration
    name:         "Calibration & Raw Prep"
    description:  "Convert calibrant .raw → CBF and generate pyFAI .poni files (pre-reduction)"
    port:         5101
    entry:        "calibration/app.py"
    icon:         "🎯"
    icon_id:      "swaxs-calibration"
    accent:       "swaxs-calibration"
    color:        "#AD1457"
    knowledge:    "calibration/knowledge.md"

  - id:           reduction
    name:         "Reduction & Correction"
    description:  "2D → 1D data reduction, normalization, PyFAI integration"
    port:         5102
    entry:        "reduction/app.py"
    knowledge:    "reduction/knowledge.md"
    manifest_key: "files"
    icon:         "🌀"          # fallback only — icon_id below is what the hub renders
    # Sprite symbol + accent token (assets/icons/). When icon_id is present the
    # hub draws <svg><use href="…#icon_id"/></svg> and takes both the mark and
    # the card's top stripe from `accent`, so the two can never disagree.
    icon_id:      "swaxs-reduction"
    accent:       "swaxs-reduction"
    color:        "#1565C0"      # still used by apps that have no icon_id yet

  - id:           average
    name:         "Visualisation & Average"
    description:  "Visualise 2D & 1D SAXS/WAXS data, average scans, stitch SAXS+WAXS"
    port:         5103
    entry:        "average/app.py"
    knowledge:    "average/knowledge.md"
    manifest_key: "files"
    icon:         "📊"
    icon_image:   "/static/average_icon.png"   # overlaid frame profiles, averaged curve on top
    icon_id:      "swaxs-visualisation"
    accent:       "swaxs-visualisation"
    color:        "#2E7D32"

  - id:           background
    name:         "Background Subtraction"
    description:  "Keyword, scan-matched, and user-defined background subtraction"
    port:         5104
    entry:        "background/app.py"
    knowledge:    "background/knowledge.md"
    manifest_key: "background"
    icon:         "➖"          # sample minus buffer/blank — literally what this app does
    icon_image:   "/static/background_icon.png"  # sample + buffer curves, subtracted result on top
    icon_id:      "swaxs-subtraction"
    accent:       "swaxs-subtraction"
    color:        "#6A1B9A"

  - id:           quality
    name:         "Quality Gate"
    description:  "AI grading of subtracted profiles — good/bad scoring, auto-sort, frame selection"
    port:         5105
    entry:        "quality/app.py"
    knowledge:    "quality/knowledge.md"
    manifest_key: "quality"
    icon:         "🚦"          # triage into Good / NeedsReview, not a single pass/fail
    icon_id:      "swaxs-quality"
    accent:       "swaxs-quality"
    color:        "#00897B"

  - id:           analysis
    name:         "Data Analysis"
    description:  "Guinier, Porod, Kratky, pair distance p(r), model fitting"
    port:         5106
    entry:        "analysis/app.py"
    knowledge:    "analysis/knowledge.md"
    manifest_key: "analyses"
    icon:         "📈"
    icon_image:   "/static/analysis_icon.png"  # SAXS profile with fitted curve overlaid
    icon_id:      "swaxs-analysis"
    accent:       "swaxs-analysis"
    color:        "#E65100"

  - id:           analyzer
    name:         "Auto-Fit & Optimiser"
    description:  "Auto-fit subtracted SAXS → size, PDI, invariant, phase, confidence (loop input)"
    port:         5107
    entry:        "analyzer/app.py"
    manifest_key: "analyses"
    icon:         "🧭"          # navigating parameter space toward the target size — the search/optimize loop
    icon_id:      "swaxs-autofit"
    accent:       "swaxs-autofit"
    color:        "#4527A0"     # NOT reduction's #1565C0 — two apps sharing one
                                # colour are indistinguishable in a 16 px tab
    knowledge:    "analyzer/knowledge.md"

  - id:           reactor
    name:         "Autonomous Synthesis"
    description:  "5-pump continuous-flow reactor control — executes predicted recipes, auto-flush"
    port:         5108
    entry:        "reactor/app.py"
    knowledge:    "reactor/knowledge.md"
    manifest_key: "reactor"
    icon:         "🔁"          # the unattended closed loop: recipe → collect → measure → next recipe
    icon_id:      "swaxs-synthesis"
    accent:       "swaxs-synthesis"
    color:        "#00838F"

  - id:           assistant
    name:         "Guinier"
    description:  "LLM-powered data interpretation, proactive hints, inline plots"
    port:         5109
    entry:        "assistant/app.py"
    knowledge:    "assistant/knowledge.md"
    manifest_key: "ai_memory"
    icon:         "🤖"
    icon_image:   "/static/saxs_smiley.png"
    icon_id:      "swaxs-guinier-ai"
    accent:       "swaxs-guinier"
    color:        "#455A64"

  - id:           watchdog
    name:         "Auto Watch"
    description:  "Loop liveness monitoring and all platform notifications"
    port:         5110
    entry:        "watchdog/app.py"
    knowledge:    "watchdog/knowledge.md"
    icon:         "📡"          # fallback only — icon_image below is what renders
    icon_image:   "/static/watchdog_icon.png"   # tools/make_watchdog_icon.py
    icon_id:      "swaxs-watch"
    accent:       "swaxs-watch"
    color:        "#D32F2F"
