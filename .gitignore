.env
python-version/
pyproject.toml
.python-version
uv.lock
atT/
demo/1D
Hor_scan_Run4_RampT20_ctr0/
atT_smaller/
.DS_Store
.hub_state.json
__pycache__
*_test/
*.log
TODO.md
.venv
Demo_Data/
venv/
.git-old-backup/

# AI-knowledge vector database - generated locally, rebuilt per machine.
ai_knowledge/vector_db/

# Per-app runtime logs written by the hub
logs/

# Local experiment data / autonomous test conditions (not part of the codebase)
1D/

# node_modules for the jsdom UI runtime checks (optional, dev only)
tests/ui/node_modules/
ai_knowledge/ingestion_log*.json

# Assistant "group layer" SOPs — runtime state created by the group_sops tool
# on first add, per docs/ASSISTANT_FUNCTIONAL_SPEC.md; deliberately not shipped.
ai_knowledge/group/

# Claude Code per-machine settings
.claude/settings.local.json

# Scratch planning notes (the work they describe lands in the commit message)
.phase2_diff.md
